# 장애물 없는 트랙 한 바퀴 자율주행

`driving_sim` 환경에서 카메라만으로 2차로 트랙의 바깥 차로(왼쪽에서 두 번째)를
따라 주행한다.

## 결과

5랩 연속 완주, 정지·이탈 없음.

| 항목 | 값 |
|---|---|
| 랩타임 | 69.1 / 66.2 / 65.9 / 66.5 / 65.9 초 |
| 차로 중앙 이탈량 | 평균 0.32 m, 90분위 0.68 m |
| 점선(중앙선) 밟음 | 8.3 % |
| 실선(바깥) 밟음 | 7.4 % |
| 인지 주기 | 약 29 Hz (GPU) |

트랙 텍스처 단면을 직접 재서 얻은 차선 기하는 이렇다.

    잔디 경계    -24.39
    바깥 실선    -24.28 ~ -24.16   (두께 0.12 m)
    점선 중앙선  -21.30 ~ -21.18   (두께 0.12 m)

차선 중심 간격 2.98 m, 선 안쪽 면 사이 실주행 폭 2.86 m. 차폭이 1.75 m 이므로
좌우 여유는 각각 0.55 m 뿐이다. 이탈량과 밟음 비율은 이 기하와 차체 네 모서리를
기준으로 잰 값이다 (`tools/lane_gt.py`).

## 실행

GPU 컨테이너에서 (`docker compose up -d hmobility-sim-gpu`):

```bash
cd /root/hmobility_ws
./tools/run_sim.sh                    # 기본값으로 실행
./tools/run_sim.sh -d v_max:=2.0      # 백그라운드, 속도 바꿔서
```

`run_sim.sh` 는 이전 실행 잔재를 정리하고 `GAZEBO_MODEL_DATABASE_URI` 를 비운다.
이걸 비우지 않으면 gzserver 가 온라인 모델 DB 를 받으러 30초를 블로킹하고,
그 사이 스폰된 차량의 카메라 센서 초기화가 타임아웃되어 영상이 나오지 않는다.

주요 런치 인자: `v_max`, `v_min`, `center_offset`, `k_ff_deficit`, `k_offtrack`,
`imgsz`, `conf`, `debug_viz`, `show_lane_debug`.

## 구성

```
카메라 → yolov8_node → lane_info_extractor → path_planner → motion_planner → 차량
                       (lane_estimator)      (스플라인)     (순수추종+앞먹임)
```

### 인지 (`camera_perception_pkg/lane_estimator.py`)

자차 차로의 좌우 경계를 따로 추정하고, 둘의 간격이 차로 폭으로 말이 될 때만
중점을 쓴다. 한쪽만 보이면 **바깥쪽 실선을 우선**한다. 잔디와 맞닿은 연속선이라
점선 중앙선보다 훨씬 안정적이다.

여러 구간에서 겪은 문제와 대응:

- **마스크 누수** — 교차 구간에서 `lane2` 마스크가 주차장 쪽으로 새면 좌측 끝이
  이미지 가장자리까지 끌려간다. 행마다 끊긴 덩어리로 나눠 차로 폭에 맞는 것만 쓴다.
- **BEV 기울기** — 차로가 기울면 가로 행으로 자른 폭이 `1/cos(기울기)` 배가 된다.
  60도면 두 배라 폭 상한에 걸려 멀쩡한 행을 버리게 되므로, 기울기만큼 상한을 늘린다.
- **클래스 오분류** — 일부 구간에서 모델이 자차 차로를 `lane1` 으로 붙인다.
  라벨 대신 '차가 들어 있는 차로 영역' 을 자차 차로로 본다.
- **다른 선 혼입** — 좌측 S자에는 주차장 진입로 표시도 `center_line` 으로 잡힌다.
  추적 중인 차로의 중앙선에서 200px 넘게 떨어진 조각은 버린다.
- **차로 혼동** — 직전 프레임에서 추적하던 차로 위치를 다음 프레임 탐색의
  출발점으로 넘긴다. 매번 차량 중심에서 시작하면 두 차로가 모두 후보일 때
  프레임마다 다른 차로를 문다.

샘플링 행은 `(20, 100, 180, 260, 330, 390, 435, 470)` 이다. 가까운 행을 반드시
포함해야 한다. 급코너에서는 길이 화각을 벗어나 먼 행이 비고, 차선 정보가 화면
맨 아래에만 남는다.

### 제어 (`decision_making_pkg/motion_planner_node.py`)

순수추종에 곡률 앞먹임을 얹었다.

- **순수추종** — 전방 주시거리 `Ld = 0.70v + 3.75`, `[4.85, 6.40]` m.
- **곡률 앞먹임 하한** — 순수추종은 원호 위에서 정확하지만, 코너 반경이 주시거리보다
  짧아지면 그 거리의 차로 중심이 존재하지 않는다 (이 트랙 최소 반경 4.5m, BEV 는
  4.74m 앞부터 보인다). 그래서 `atan(축간거리 × 곡률)` 을 하한으로 깐다.
- **뒷축 off-tracking 보정** — 코너에서 뒷축은 앞축보다 `축간거리² × 곡률 / 8`
  만큼 안쪽을 지난다. 실측에서도 뒷축이 0.13m 더 안쪽이었다. 그만큼 조준점을
  바깥으로 민다.
- **연속 조향** — `MotionCommand.steering` 은 int32 -7..7 이라 한 칸이 5.29도로
  성기고, 그 단차가 그대로 조향 떨림이 된다. 시뮬레이션에서는 연속값이 허용되므로
  `steering_angle` (Float32, rad) 토픽을 따로 두고 `simulation_sender_node` 가
  그쪽을 우선 쓴다. 실차(오프라인 실습)는 아두이노가 정수만 받으므로
  MotionCommand 경로를 그대로 유지하고, 그쪽은 시그마-델타로 시간평균을 맞춘다.
  조향 표본간 변화 0.57 -> 0.45칸, 방향 반전 331 -> 206회.
- **직진 락온** — 직선에서 GT 도로곡률로 계산한 이상 조향은 -0.02칸, 사실상 0 이다.
  그런데 인지 곡률은 직선에서도 표준편차 0.080 이라 앞먹임이 ±2.45칸의 잡음을
  그대로 조향에 넣는다. 순수추종 항의 잡음(±0.67칸)보다 4배 크다. 직진 우블링의
  정체가 이것이다. 그래서 곡률이 충분히 작으면(|k| < 0.045) 앞먹임을 아예 끄고
  순수추종 되먹임만 쓴다. 해제 문턱을 따로 둬(0.070) 경계에서 깜빡이지 않게 한다.
  앞먹임을 꺼도 횡오차 되먹임은 살아 있으므로 차로는 계속 유지된다.
- **평활** — 최종 조향에 1차 저역통과 한 번만 건다 (`TARGET_LPF`). 여러 곳에
  나눠 걸면 지연만 쌓인다. 연속 조향으로 바꾼 뒤 정수화 사각지대가 없어져
  0.45 -> 0.28 로 더 세게 걸 수 있게 됐다. 0.18 까지 낮추면 더 부드럽지만
  지연이 커져 바깥 실선을 밟는다 (4.8 -> 10.4%).

속도는 곡률로 정한다. GT 곡률을 재보니 이 트랙 최소 반경이 4.5m 라, 횡가속도
3.5 m/s² 기준으로는 2.4 m/s 에서 감속할 일이 없다. 실제 제약은 조향 여유이므로
조향 요구가 4.5칸을 넘을 때만 줄인다.

## 평가 도구

지상진실은 트랙 텍스처(`basic_track_2026.jpg`)에서 만든다. 제어에는 쓰지 않는다.

```bash
python3 tools/lane_gt.py        # 차로 중앙 이탈량, 점선/실선 밟음 비율
python3 tools/diagnose.py       # 총 오차를 인지 오차와 제어 오차로 분해
python3 tools/steer_eval.py     # GT 곡률로 만든 이상 조향과 실제 조향 비교
python3 tools/speed_profile.py  # 곡률에서 물리적 최속 프로파일과 랩타임 하한
python3 tools/gt_viz.py         # 기준 차선과 주행 궤적을 그림으로 저장
python3 tools/perception_noise.py 0 -22.73 0   # 정지 상태에서 인지 잡음 (drive_enable:=false 필요)
```

`lane_gt.py` 는 차로 폭을 상수로 가정하지 않는다. 바깥 실선과 점선 중앙선까지의
거리를 각각 재서 그 중점을 차로 중앙으로 삼고, 차체를 한 점이 아니라 앞축·뒷축의
좌우 끝 네 모서리로 평가한다. 코너에서 뒷축이 안쪽을 파고드는 것은 한 점으로는
잡히지 않는다.

`diagnose.py` 의 분해가 개선 방향을 정하는 데 가장 유용했다.

```
인지 오차 = (인지가 말한 차로 중심) - (진짜 차로 중심)
제어 오차 = (차량 위치) - (인지가 말한 차로 중심)
```

## 시도했다가 되돌린 것

지표가 있으니 가설을 세우고 재서 판단했다. 아래는 모두 5랩으로 재서 악화가
확인되어 되돌린 것들이다. 기록해 두는 편이 같은 길을 다시 가지 않는 데 낫다.

- **곡률 추정 재설계** (기저 확대 + 스플라인 끝단 제외 + 잔차 게이팅).
  직선에서의 곡률 편향은 -0.068 -> -0.012 로 확실히 잡혔는데, 정작 주행은
  나빠졌다 (이탈량 0.39 -> 0.55m, 실선 밟음 1 -> 17%).
  곡률 편향과 `CENTER_OFFSET` 이 서로 상쇄하며 균형을 이루고 있었고, 편향만
  없애자 균형이 깨졌다. 잡음(0.080 -> 0.061)은 거의 줄지 않았는데, 잡음이야말로
  흔들림의 본체였다.
- **경로를 최소제곱 2차로 맞추기.** 스플라인이 타겟 지점을 정확히 지나 잡음이
  경로에 남는 것을 없애려 했으나, 8개 점 자체의 잡음이 2차 계수에 그대로 남아
  곡률 잡음이 줄지 않았고 경로 표현력만 잃었다 (점선 밟음 19.8 -> 28.2%).
- **오차 점진 이득** (작은 오차에 약하게 반응). 작은 오차에 늦게 반응하니
  점선 쪽으로 더 붙었다 (19.8 -> 26.7%, 문턱 0.45 에서는 33.5%).
- **속도 낮추기** (2.4 -> 2.1 m/s). 주시거리가 `0.7v + 3.75` 라 속도를 낮추면
  주시거리도 짧아져 이득이 커진다. 오히려 나빠졌다 (17.0 -> 22.7%).
- **곡률에 따라 주시거리 줄이기.** 짧은 주시거리는 순수추종의 이득을
  키워(delta ~ lat/Ld^2) 코너 안쪽을 더 파고들었다.
- **앞먹임 곡률 압축** (`kappa/(1+c|kappa|)`). 조향은 극적으로 부드러워졌지만
  (방향 반전 217 -> 29회) 코너 조향까지 같이 죽어 바깥으로 밀렸다
  (실선 밟음 c=0.8 에서 15.7%, c=3.0 에서 25.2%). 우블링의 크기가 앞먹임 크기에
  직결된다는 것은 확인됐고, 그래서 직진에서만 끄는 락온 방식으로 갔다.

측정에는 주행 간 편차가 있다. 같은 설정으로 다시 돌리면 점선 밟음이 1~2%p,
방향 반전이 20~30% 흔들린다. 그보다 작은 차이는 유의하지 않다고 봐야 한다.

## 남은 흔들림의 구조적 원인

조향, 실측 요레이트, 전방 주시점의 횡방향이 **모두 3.2초 주기로 함께** 흔들린다.
인지 잡음이 그대로 흘러나오는 것이 아니라 폐루프 한계진동이다.

전방 주시 시간이 `5.4m / 2.4m/s = 2.25초` 이고 진동 주기가 그 1.4배인데,
이는 예측제어 한계진동의 전형적인 비율이다. 그리고 **주시거리를 2.25초보다
짧게 잡을 수가 없다.** BEV 가 뒷차축 앞 4.74m 부터 보이기 때문이다.

이 구조에서 시도할 수 있는 레버는 다 재봤고, 모두 듣지 않았다.

| 시도 | 결과 |
|---|---|
| 속도 낮추기 (2.4 -> 2.0, 주시거리 고정) | 점선 밟음 8.3 -> 15.1%, 랩타임 82초 |
| 주시거리 늘리기 (5.4 -> 6.0 고정) | 8.3 -> 9.4%, 사실상 동일 |
| 요레이트 되먹임 감쇠 | 8.3 -> 10.8%, 조향 활동 0.60 -> 1.11 로 악화 |
| 앞먹임 곡률 압축 | 조향은 부드러워지나 코너 조향이 죽어 바깥으로 밀림 |

요레이트 감쇠가 듣지 않는 이유는 분명하다. 차량의 요레이트 이득이 0.73 이라
지령과 실측 사이에 상시 오차가 남고, 감쇠항이 그 정상 오차를 계속 밀어내며
활동만 늘린다.

### 인지 잡음은 생각보다 훨씬 작다

한동안 "전방 주시점의 잡음이 0.30 m 라 그것이 근본 원인" 이라고 적었는데,
**그 값은 움직이는 차에서 잰 것이라 틀렸다.** 차의 실제 이동이 잡음으로 섞여
있었다. `drive_enable:=false` 로 차를 완전히 세우고 다시 재면 이렇다
(`tools/perception_noise.py`, 프레임간 표준편차 [px]).

| 지점 | 행 20 | 100 | 180 | 260 | 330 | 390 | 435 | 470 |
|---|---|---|---|---|---|---|---|---|
| 아랫변 (0, -22.7) | 0.4 | 0.5 | 0.3 | 0.1 | 0.2 | 0.3 | 1.6 | **122.4** |
| 오른쪽 (16.7, 0) | **28.4** | **28.7** | **44.7** | 3.2 | 0.6 | 0.2 | 0.2 | 0.4 |
| 윗변 (1.2, 23.0) | 0.8 | 1.0 | 0.2 | 0.1 | 0.3 | 0.0 | 0.1 | 0.0 |

**대부분의 지점에서 인지는 사실상 결정론적이다 (0.0~1.0 px).**
잡음은 특정 위치에서만, 특정 행에서 터진다. 행 자체의 문제가 아니라 그 장면에서
마스크가 애매해지는 것이다. 실제로 가장 불안정하던 행(470)을 빼봤더니 오히려
나빠졌다 (실선 밟음 5.0 -> 20.4%).

그러므로 주행 중 흔들림은 인지 잡음이 아니라 **폐루프 한계진동**이 본체이고,
여기에 특정 구간의 국소적 인지 흔들림이 겹치는 구조다.

다음에 손댈 곳은 그 국소 구간이다. 좌측 S자와 오른쪽 직선의 먼 행에서
무엇이 마스크를 애매하게 만드는지 프레임을 저장해 보는 것이 출발점이다.
`SRC_MAT` 재캘리브레이션으로 BEV 를 더 가까이 보이게 해 주시거리를 줄이는 것도
한계진동 주기를 짧게 만드는 방향이라 시도해 볼 만하다.

버전별 성적과 되돌리는 방법은 [VERSIONS.md](VERSIONS.md) 에 있다.

## 남은 것

- 점선 밟음 8.7%, 실선 밟음 4.7%. 실주행 폭 2.86m 에 차폭 1.75m 라 좌우 여유가
  0.55m 뿐이어서, 이탈량 0.30m 면 밟는 순간이 생긴다.
- 좌측 S자는 랩마다 편차가 있다. 불량 랩은 전방점 횡방향의 표준편차가 크다
  (정상 0.54~0.86 m vs 불량 0.99~1.28 m).
- 이론 랩타임 하한은 58.1초 (`tools/speed_profile.py`). 현재 65초대다.

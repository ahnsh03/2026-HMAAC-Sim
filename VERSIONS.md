# 버전 기록

주행 품질이 좋았던 버전들. 되돌려서 다시 돌려볼 수 있도록 남긴다.

## 공정한 비교에 관하여

세션 중 평가 지표가 여러 번 바뀌었다. 그래서 **커밋 메시지에 적힌 수치끼리는
비교할 수 없다.** 아래 표는 네 버전을 모두 **현재 지표**(`tools/lane_gt.py`,
차선 두께와 차체 네 모서리를 반영한 것)로 다시 재서 얻은 값이다.

측정 방법은 각 버전의 `src/` 만 바꿔 끼우고 `tools/` 는 현재 것을 유지한 뒤
기본 인자로 4랩씩 주행한 것이다.

```bash
git checkout <커밋> -- src/     # tools/ 는 건드리지 않는다
./tools/run_sim.sh -d
python3 tools/lane_gt.py
```

## 순위

| 순위 | 커밋 | 이탈\|평균\| | 90분위 | 점선 밟음 | 실선 밟음 | 합계 | 조향 표본간변화 | 방향 반전 |
|---|---|---|---|---|---|---|---|---|
| 1 | `a8c8139` | **0.25 m** | **0.49 m** | 4.9 % | 2.8 % | **7.7 %** | 0.620 | 291 |
| 2 | `b6b5962` | 0.28 m | 0.57 m | 5.3 % | 7.1 % | 12.4 % | **0.446** | 236 |
| 3 | `104ac7d` | 0.28 m | 0.56 m | 6.7 % | 5.0 % | 11.7 % | 0.456 | **228** |
| — | `88bed55` | 0.30 m | 0.63 m | 9.5 % | 2.2 % | 11.7 % | 0.619 | 253 |

랩타임은 네 버전 모두 65~70초로 차이가 없었다.

## 무엇을 고를 것인가

**차로 유지가 최우선이면 1위 `a8c8139`.** 이탈량과 밟음 비율이 확실히 낫다.
대신 조향 명령이 정수 -7..7 이라 한 칸이 5.29도로 성기고, 그만큼 조향이 거칠다.

**주행감이 최우선이면 2·3위.** 연속 조향을 쓰므로 조향 표본간 변화가 0.45 로
1위(0.62)보다 눈에 띄게 부드럽다. 차로 유지는 조금 양보한다.

즉 **차로 유지와 조향 부드러움은 이 구조에서 맞바꾸는 관계**다. 한쪽을 얻으면
다른 쪽을 조금 내주게 된다.

## 각 버전의 내용

### 1위 — `a8c8139` "GT 기반 조향 평가를 추가하고 조향 평활을 한 곳으로 모은다"

정수 조향 시절의 마지막 버전.

- 조향 명령은 `MotionCommand.steering` (int32 -7..7). 시그마-델타로 정수화하며
  버린 몫을 다음 tick 으로 넘겨 시간평균을 맞춘다.
- 흩어져 있던 저역통과를 최종 조향 한 곳으로 모았다 (`TARGET_LPF = 0.45`).
- `STEER_GAIN = 1.00`, `LANE_WIDTH = 328 px`, `CENTER_OFFSET = 50 px`.

### 2위 — `b6b5962` "시뮬레이션에서 연속 조향을 쓰고, 조향 평활을 다시 잡는다"

- `steering_angle` (Float32, rad) 토픽을 새로 두고 `simulation_sender_node` 가
  그쪽을 우선 쓴다. 정수 15단계 제약이 사라진다.
  실차용 `MotionCommand` 경로는 그대로 남겨 두어 오프라인 실습에 영향이 없다.
- 정수화 사각지대가 없어져 `TARGET_LPF` 를 0.45 -> 0.28 로 더 세게 걸었다.

### 3위 — `104ac7d` "직진 구간에서 곡률 앞먹임을 끄는 락온을 넣는다"

2위에 더해,

- 곡률이 작으면(`|k| < 0.045`) 곡률 앞먹임을 아예 끄고 순수추종 되먹임만 쓴다.
  해제는 0.070 에서 한다 (히스테리시스).
  직선에서 인지 곡률은 표준편차 0.080 의 잡음인데 (참값 0), 그것이 앞먹임을 통해
  ±2.45칸의 조향 잡음이 되고 있었다.

### 현재 `88bed55` — 평가 기하 정정 + 요레이트 이득 보상

평가 도구는 이 버전이 가장 정확하므로 **`tools/` 는 항상 최신을 쓴다.**
다만 제어 쪽 변경(`STEER_GAIN` 1.00 -> 1.15)은 정정된 지표에서 이득이 없었다.

- `tools/lane_gt.py`: 차선 두께 0.12m, 잔디~실선 여백 0.11m, 차로 중앙 기준의
  0.115m 바깥 치우침을 모두 보정. 실선 밟음을 잔디가 아닌 선 안쪽 면 기준으로.
- `LANE_WIDTH` 328 -> 309 px (차선 중심 간격 2.98m 실측).
- `STEER_GAIN` 1.15 — 요레이트 실측/지령 이득이 0.73 이라 보상하려 했으나,
  정정된 지표로는 조향만 거칠어졌다 (0.456 -> 0.619).

## 되돌리는 방법

```bash
# 제어·인지만 특정 버전으로 (평가 도구는 최신 유지)
git checkout a8c8139 -- src/

# 완전히 그 시점으로
git checkout a8c8139

# 현재로 복귀
git checkout main -- src/
```
